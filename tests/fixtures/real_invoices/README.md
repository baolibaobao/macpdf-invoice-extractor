# Real invoice PDF samples

Place real native digital invoice PDFs in this folder for local manual testing.

These files may contain private invoice information, so do not commit real PDFs
to Git. Keep only this README and `.gitkeep` under version control.
