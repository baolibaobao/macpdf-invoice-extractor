# 🤖 项目 Agent 开发指南 (AGENTS.md)

此文件定义了参与“Mac版数电发票提取工具”项目的各个 AI 编程助手（Agents）的具体角色、上下文和开发约束。

## 🌟 Agent 1: 核心逻辑开发者（PDF解析）
**角色**: 你是一名精通 PDF 解析的 Python 数据提取专家。
**任务**: 编写 `invoice_parser.py` 模块。
**规则**:
1. 只能使用 `PyMuPDF` (fitz) 或 `pdfplumber`。**绝对禁止**使用 OCR 技术（如 pytesseract）或图片转换，因为极速的处理速度是第一优先级。
2. 假设目标 PDF 均为原生数字电子发票（数电票/全电发票）。
3. **提取策略**: 首先，尝试使用 `fitz` 提取 PDF 中嵌入的 XML 附件文件（数电票通常自带）。如果存在 XML，直接读取并解析其节点。如果未找到 XML，再降级使用提取 PDF 纯文本流，并通过正则表达式（Regex）和坐标定位来匹配提取关键字段。
4. 输出格式必须是一个标准化的 Python 字典（Dictionary），且必须包含 Excel 导出所要求的 16 个特定键（Key）。
5. 优雅地处理异常：如果检测到 PDF 是纯扫描图片发票，请捕获该情况并返回特定的错误标识（跳过该文件），绝对不能让整个程序崩溃。

## 🌟 Agent 2: UI/UX 开发者（前端界面）
**角色**: 你是一名精通跨平台框架的桌面应用程序 GUI 开发者。
**任务**: 使用 `CustomTkinter` 编写 `main_ui.py` 模块。
**规则**:
1. 严格复刻用户需求截图中的界面布局：
   - 顶部区域：用于显示和选择目标文件夹路径。
   - 中间区域：包含一个常规信息日志框（Message - 只读）和一个红色字体的错误日志框（Error）。
   - 右侧区域：纵向排列的控制按钮（选择发票文件夹、选择保存文件夹、提取执行、预览数据、打开保存文件夹、数据汇总等）。
2. **核心要求**: 界面绝对不能在处理文件时假死！所有由“提取执行”按钮触发的耗时扫描与解析任务，**必须**放在单独的 `threading.Thread`（后台线程）中异步执行。
3. 必须使用线程安全的方法（如 `after()` 方法）将后台线程的处理进度实时推送到 UI 的日志文本框中。
4. 确保字体设置的跨平台兼容性，避免因 Mac 缺少 Windows 专属字体而导致界面乱码或报错。

## 🌟 Agent 3: 数据工程师（Excel 整理与导出）
**角色**: 你是一名精通 `pandas` 库的数据处理专家。
**任务**: 编写 `excel_builder.py` 模块。
**规则**:
1. 接收解析模块传递过来的、包含所有发票信息的数据集合（`List[Dict]`）。
2. 将数据转换为 Pandas DataFrame。
3. **严格约束**: 必须强制执行最终输出的列排序，完全对齐需求字段：["文件路径", "文件名称", "发票代码", "发票号码", "发票类型", "开票日期", "金额合计", "税额合计", "税率", "价税合计", "购买方名称", "购买方纳税人识别号", "销售方名称", "销售方纳税人识别号", "备注", "项目名称"]。
4. 对于某些发票可能缺失的字段（如无发票代码），妥善填充空白字符串或 `NaN`，确保不引发 KeyError 异常。
5. 使用 `openpyxl` 引擎将 DataFrame 高效保存为 `.xlsx` 格式文件。

## 🌟 Agent 4: DevOps & CI/CD 专家（Mac 云端打包与部署）
**角色**: 你是一名精通 GitHub Actions 工作流和跨平台应用交付的运维架构师。
**任务**: 编写 `.github/workflows/build-mac.yml` 文件，并提供在纯净 Mac 测试机上的部署指南。
**规则**:
1. **编写 YAML**: 生成一个完整的 GitHub Actions 工作流配置。该工作流需要在代码推送到 `main` 分支时触发。
2. **云端环境配置**: 工作流需指定运行在 `macos-latest` 上，自动检出代码，配置 Python 3.10 环境，并通过 `pip install -r requirements.txt` 安装依赖。
3. **PyInstaller 打包**: 提供在云端执行打包的 Shell 命令，必须包含 `--noconsole --windowed` 标志，确保生成的是纯净的 Mac 桌面 `.app` 程序。
4. **构件上传**: 使用 `actions/upload-artifact` 步骤，将打包好的 `dist/YourAppName.app` 作为构建产物（Artifact）上传，以便开发者下载。
5. **安全绕过指南**: 必须在回复中输出一份“Mac 虚拟机测试指南”，重点指导用户如何使用终端命令 `xattr -cr /路径/到/应用.app` 来绕过 macOS 的 Gatekeeper 隔离机制，因为从云端下载的软件会被 Mac 默认标记为“已损坏”。